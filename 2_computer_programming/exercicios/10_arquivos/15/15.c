# include <stdio.h>

int main() {

    FILE *arquivo = fopen("frases.txt", "r");

    contador = 0;
    char linha[100];
    while(fgets(linha, sizeof(linha), arquivo) != NULL) {
        
    }

    return 0;
}